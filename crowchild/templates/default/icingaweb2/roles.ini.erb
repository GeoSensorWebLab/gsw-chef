# /etc/icingaweb2/roles.ini

[admins]
users               = "<%= @admins.join(",") %>"
permissions         = "*"

[readers]
users 				= "<%= @readers.join(",") %>"
permissions			= "module/monitoring"
